import React from 'react';

import classes from './drawerToggle.module.css';

const drawerToggle = (props) => (
    <div className={classes.drawer__toggle}>
        <div></div>
        <div></div>
        <div></div>
    </div>
);

export default drawerToggle;